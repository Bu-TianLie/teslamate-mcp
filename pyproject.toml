[project]
name = "teslamate-mcp"
version = "0.1.0"
description = "MCP server for reading TeslaMate charging sessions and writing charging costs."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.0",
    "asyncpg>=0.29.0",
    "python-dotenv>=1.0.1",
]
